/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true, // Habilita la minificación del código utilizando SWC para mejorar el rendimiento.
};

export default nextConfig;
